# 观念年轮 · 项目交接文档

> 首次盘点：2026-09-12 傍晚 · 最后更新：**2026-09-15 03:45** · 工作区：`F:\work palce`
> 冲刺截止 **09-15 10:00**。先看 §0；**§2.6 是 AI 接入 / 输入推荐 / 独立部署**，**§2.7 是速度实测**，**§7.1 是刚修掉的抽句硬伤**。
> 本文所有路径、命令、状态均实际核对过，不是凭记忆写的。

---

## 0. 如果你只有五分钟

1. **截止时间**：知乎黑客松 2026｜校园新锐季，赛道「知识炼金场｜学习工具与知识生产」，冲刺窗口 2026-09-13 10:00 → **09-15 10:00**。
2. **三件交付物**：加载过场动画 `liukanshan/liukanshan-loader.html`、项目封面 `poster-v2/poster-gen.html`（315×175，**已用于报名，不进网站**）、**网页正片 `nianlun-web/`（能跑：`node build-index.mjs && python server.py 8765`）**。
3. **网页正片是现在的重心**，09-14 一天改了三轮：开场动画接进去了 → 布局改成「上下分区 + 整页可滚」→ 年份改成「绕整圆均匀排列、圆心居中」。详见 §2.5。
4. **09-15 修掉了「抽句抽歪」这个硬伤**。原来摘出来当总结的那句话经常跟用户输入毫无关系（实测 11 年里 7 年零命中查询词）。现在抽句改成「按命中查询词排序」，再让 AI 在候选池里挑一句 —— **AI 只返回下标，句子仍是知乎原文**，不可能编造引语。详见 §7.1。
5. **09-15 接好了独立 AI**：`ai.py` 走一个 OpenAI 兼容端点，不依赖本机任何东西；`server.py` 的绑定地址、端口、知乎 CLI 路径、模型、密钥全部走环境变量，**整个 `nianlun-web/` 可以直接拷到服务器上跑**。详见 §2.6。
6. **09-15 加了输入推荐**：首屏输入关键词，**0.9–1.6s** 出 8 条真实标题，点一条直接查。靠的是只发**一条**搜索调用（不是那 15 条扇出）。详见 §2.6。
7. **`index.html` 是生成产物**，改样式要动 `build-index.mjs` 再重跑，别直接手改。
8. **本环境查不到知乎网页**（403），所以「把文案换成可核实的真实引语」这条路在这个环境里走不通，详见 §6。但 `nianlun-web` 走的是知乎官方 CLI，能拿到真实数据，缓存里六句话都是真查回来的。

---

## 1. 项目是什么

**观念年轮** —— 追踪一个「观念」的共识如何随时间涨落。

- 一句话定位：**你的观点，还剩多少保质期**
- 核心主张：**每一个常识，都曾经是少数派**
- 使用方法：输入一句你信以为真的常识（示例：「买房是最好的投资」），它去知乎把这句话历年的讨论捞回来，按年份排成一个年轮 —— 从正上方起顺时针一年一格，点越大说明那年谈的人越多。点年份看当年的原文，链接直达知乎可核。
- 参赛：知乎黑客松 2026｜校园新锐季 · 知识炼金场｜学习工具与知识生产
- 交付边界：**09-12 那版**只负责加载动画与封面两件物料；**09-13 起扩展**，`nianlun-web/` 把检索、聚合、可视化都做了，是现在的主体。

---

## 2. 交付物清单

### 2.1 加载过场动画

| 文件 | 是什么 | 状态 |
|---|---|---|
| `liukanshan/liukanshan-loader.html` | **主交付**。单文件零外部依赖（字体 base64 内嵌） | ✅ 已交付 |
| `liukanshan/gen-loader.mjs` | 生成器，产出上面那个 HTML | ✅ |
| `liukanshan/loader-delivered.png` | 交付状态截图（2x） | ✅ |
| `liukanshan/win-desktop.png` / `win-laptop.png` / `win-phone.png` | 1591×916 / 1280×800 / 390×844 实机截图 | ✅ |
| `liukanshan/font-BodoniModa.woff2` | 年份数字字体（3.7 KB，子集化到 0-9） | ✅ 生成器依赖 |
| `liukanshan/liukanshan_desert.*.svg` | 官方素材：沙漠站立 | ✅ |
| `liukanshan/liukanshan_wire.*.svg` | 官方素材：被电线缠住（**动画与封面当前都用这张**） | ✅ |

**对外 API**（网页里直接调）：

```js
LiukanshanLoader.start();        // 自行起跑：年份 2010→2026，先快后慢
LiukanshanLoader.progress(0..1); // 用真实加载进度驱动年份
LiukanshanLoader.done(cb);       // 定格 2026，淡出后回调
```

**版式契约**（已机器验证）：数字与主体**上下错开、永不重叠**；主体居中，云朵落在数字一半高度；横线中心对齐文案中轴；任意视口不裁切。

### 2.2 项目封面

| 文件 | 是什么 | 状态 |
|---|---|---|
| `poster-v2/poster-gen.html` | **主交付**。含素材 base64，单文件零外链 | ✅ 已交付 |
| `poster-v2/cover-gen-2x.png` / `-1x.png` | 出图（630×350 / 315×175） | ✅ |
| `poster-v2/poster-line.mjs` | 生成器（三个版本共用一套版式与装饰） | ✅ |
| `poster-v2/poster-wire.html` / `cover-wire-*` | 备选：官方 wire 版主体 | ✅ 同步 |
| `poster-v2/poster-line.html` / `cover-line-*` | 备选：官方 desert 版主体 | ✅ 同步（**用户已否，理由：去年别人用过**） |
| `poster-v2/poster-a.html` / `cover-a-*` | **早期被否的方案 A**（功能主义网格） | ⚠️ 保留作对照，不要交 |
| `poster-v2/TitleSerif.woff2` | 标题字体 Noto Serif SC 900，**服务端子集化到「观念年轮」四字，1.7 KB** | ✅ |

### 2.3 素材与提示词（给继续生图用）

| 文件 | 是什么 |
|---|---|
| `gen-assets/fox-hourglass.png` | **用户生图处理后的成品**：420×357，已裁水印 + 重映射到封面纸色 + 镜像 |
| `gen-assets/fox-hourglass-hi.jpg` | 用户提供的原图（2732×1535） |
| `gen-assets/_remap.py` | 处理脚本：裁水印 → 镜像 → 灰度线性映射到 `--ink/--paper`（**曲线映射，不是硬阈值**，保留抗锯齿）→ 裁到内容 → 降采样 |
| `gen-refs/prompt-global.md` | **全局提示词**（中英双版 + 5 个姿势行 + negative prompt + 验收五条） |
| `gen-refs/prompt.md` | 更详细的提示词与角色设定说明 |
| `gen-refs/ref-desert.png` / `ref-wire.png` | 角色参考图（喂给生图模型锁设定） |
| `gen-refs/ref-style.png` | 风格参考图（我给的目标风格） |
| `gen-refs/ce69f27d…cthumb_wm1.png` | 你发的**第一张生图原档**（384×216 带水印缩略图，已弃用，留作对照） |

### 2.4 研究存档（非交付，供查证）

| 文件 | 是什么 |
|---|---|
| `zhihu-ring/hackathon.json`（58 KB） | 往届黑客松 135 个参赛项目 |
| `zhihu-ring/details.json`（57 KB） | 上述项目详情 |
| `zhihu-ring/ring.json`（3.5 KB） | 赛事元数据（含冲刺起止时间戳） |
| `_artist-design/*.md`（8 份） | 早期提取的 dsh-artist-mode 技能文档（反 AI slop 铁律 11 条 / 风格库 / 评审指南） |
| `nianlun-video/nianlun.mp4` / `.webm`（1.7 / 3.5 MB） | 早期 HyperFrames 渲染的动画视频（**仅供参考，未被采用**：它只能做视频，跟不上真实加载进度） |

---

### 2.5 网页正片（nianlun-web，09-13 起新增）

前面两件是物料，这件是**能跑的东西**：输入一句话，从知乎捞回原文，按年份排成年轮。

| 文件 | 是什么 | 状态 |
|---|---|---|
| `nianlun-web/index.html` | **生成产物，别手改** —— 由 `build-index.mjs` 产出。页面骨架 + 全部样式 + 内联的开场动画，零外链 | ✅ |
| `nianlun-web/build-index.mjs` | 生成器：从 `liukanshan/liukanshan-loader.html` 提取开场动画、浅色化、拼进 `index.html` | ✅ |
| `nianlun-web/app.js` | 状态机（开场→首屏→检索→结果）、圆环几何解算、年轮绘制、文章区渲染 | ✅ |
| `nianlun-web/server.py` | 服务端：知乎检索 + 分年聚合 + 抽句（启发式）+ 立场启发式 + HTTP 接口 | ✅ |
| `nianlun-web/ai.py` | **AI 层（09-15 新增）**：OpenAI 兼容客户端，只用标准库；模型只做「判断」和「选择」 | ✅ |
| `nianlun-web/warm_ai.py` | **AI 预热工具（09-15 新增）**：演示前跑一次，之后每次搜索都命中缓存 | ✅ |
| `nianlun-web/liukanshan-loader.html` | 从 `liukanshan/` 复制来的开场动画原件，`build-index.mjs` 的输入 | ✅ |
| `nianlun-web/cache/*.json` | **六句话**的真实检索存档（知乎原始返回，非编造） | ✅ |
| `nianlun-web/t2.json` / `test.json` / `_t.html` | ⚠️ 调试遗留，可删，不影响运行 | 待清 |

缓存里现有六条：买房是最好的投资 / 女性不被定义 / 孝顺是一种过时的观念 / 读研一定比本科好找工作 / 张雪峰是对的吗 / 张雪峰说的对吗。首屏示例按钮只列了前四条。

**跑起来**：

```powershell
cd "F:\work palce\nianlun-web"
python server.py 8765        # → http://127.0.0.1:8765
```

**改了页面要重新生成**：`index.html` 是产物，改样式/结构要动 `build-index.mjs`，然后

```powershell
node build-index.mjs         # → 重写 index.html
```

只改交互逻辑则直接编辑 `app.js`，不用重新生成。

**几个当时踩出来的决定**（都写在代码注释里）：

- **一句话扇出成 15 条查询**再合并去重。只发一条的话，知乎强烈偏向近期，49 条里 40 条落在当年 —— 画出来是「今年的横截面」，不是年轮。
- **串行执行，撞限流立刻收手不重试**。15 条并发打出去直接 `Code 30001`，是配额窗口级的，等 150 秒都没缓过来。
  **09-15 修正**：当初由此加的 **1.2s 间隔是过度保守的，已降到 0.15s**。实测限流卡的是**并发**、不是串行速率 —— 15 条一条接一条零间隔打出去，**15/15 成功、零 30001**，墙钟从 28.1s 掉到 11.9s。那 1.2s 白占了整条扇出 60% 的时间。并发仍然安全，因为 `_pace()` 的锁是全局的（多个用户同时查也只有 1 条 CLI 调用在飞）。详见 §2.7。
- **默认走缓存**。让人干等几十秒去凑一次可能还撞限流的实时检索，不如直接给此前真查回来的结果；页面上如实标注「走的本地存档」。
- **总结句一律从原文整句抽取**，不生成不改写，抽不到合规长度就截断并在前端标明。沿用封面那条判据：「不许添加来源没有的事实或引语」高于「要具体」。**但「抽哪一句」这一步现在是坏的 —— 只按长度筛，跟查询词毫无关系，见 §7.1。**
- **立场判断是数关键词的启发式**，前端明写「不作数」，不当统计结论卖。
- **正反两向查询词都投**，让同一年的支持与质疑都进池子 —— 只留说对的那几个等于骗人，这是项目立意本身。

**年轮版式（09-14 最终版：圆环）**：

- **圆心落在这块框的正中**（`cx = vw/2, cy = vh/2`）。上一版故意偏右上（`cxr=0.58 / cyr=0.30`）是为了给右边的文章浮层让位；文章改成下分区之后这个理由没了，偏心反而显得整块没对齐。
- **年份绕整圆均匀排开**：从正上方（12 点）起，顺时针一年一格，转满 360°。选中态仍是实心蓝，条数仍是年份下面一行小字。
- **半径不能只按「框的短边除以二」一刀切**。标签是横排两行字（年份 + 条数），**在上下两端吃掉的是它的高度、在左右两端吃掉的是它的宽度**，差一倍多。所以逐年按它自己的角度算标签盒在半径方向的半投影，再取所有年份里最紧的那个当上限：

  ```
  reach = 节点半径 + gap + (|cosθ|·半宽 + |sinθ|·半高)
  R ≤ (cx - margin - 半宽) / |cosθ| - reach
  R ≤ (cy - margin - 半高) / |sinθ| - reach
  ```

  这样圆的每一段都恰好顶到框的边界，不会一边浪费、一边越界。实测 1440×900 下 R≈237（直径约 474）。
- **标签用 `text-anchor: middle` 居中对齐**，这不是审美选择：上一版弧线布局用「左半边右对齐、右半边左对齐」，在中轴处会出现两个标签**背靠背贴死**的缝（1024×768 实测 2019/2020 右缘与左缘同为 `x=336`，差 0.1px）。绕整圈时那条中轴正好落在圆心正上/正下，居中对齐从构造上就没有这个缝。
- **背景环线按「框的长边」铺开**（`GEO.tex = 0.95`），超出部分由 `overflow:hidden` 裁掉。圆心居中之后圆本身不大，纹理只铺到圆外一圈的话，宽屏两头会空一大片。
- 年轮区高度 `clamp(520px, 66vh, 700px)`：比白天那版高，因为居中的圆完全受框高限制，框矮了圆就小。

**交互流（09-14 最终版：上下分区 + 整页可滚）**：

1. **打开动画**：进站立刻播放 `liukanshan-loader.html` 的刘看山动画 —— 从 `liukanshan/` 提取 CSS/HTML/JS 内联进主页面，色板浅色化（`--lk-bg:#FBF9F4`，深色底全去，暖黄光晕换成知乎蓝）。年份 2010→2026 滚完后停 1 秒再交棒。
2. **首屏**：搜索框大号居中（`#hero` 的可见性由 JS 写内联 opacity，不依赖 CSS 兄弟选择器 —— `opening.remove()` 后选择器失配是踩过的 bug）。
3. **搜索中**：搜索框上移成顶栏，年轮那块画呼吸空圈，文章区清空。
4. **结果**：年份绕圆画出，**默认选中最近一年**，文章区就在年轮下面。

```
固定顶栏（搜索框常驻）
  ↓
状态行      「翻到 206 条，横跨 2016–2026 年，共 11 圈。点圆上的年份，下面换那一年的原文。」
  ↓
年轮区      一块长方形，边界之外由 overflow 裁掉
  ↓
文章区      常驻，居中 860px 窄栏，搜完就摆出最近一年
```

- **`#main` 用 `display:none/block` 切换而不是 opacity**：用 opacity 的话没搜索时它也占着高度，首屏背后会多出一条滚动条。
- **搜完默认选中最近一年**。白天那版是「不默认弹浮层，先让人看清整条弧」，结果用户搜完只看到一个孤零零的年轮，以为没结果 —— 这是被用户当场否掉的设计。
- **点年份自动滚到文章区**，滚到「文章区顶边正好停在顶栏下面」（实测 `scrollY 0→644`，文章区顶边落在 `y=76`）。**但搜完那一次不滚** —— 一搜索就把年轮顶出屏幕，人连自己搜的那句话什么形状都没看清。

生成入口：`build-index.mjs`（`node build-index.mjs` 重生成 `index.html`，会重新从 `liukanshan-loader.html` 提取动画并浅色化）。`liukanshan-loader.html` 已复制到 `nianlun-web/` 供 server.py 静态 serve。

**踩过的坑：提取 IIFE 必须补回闭合**

`build-index.mjs` 提取动画 JS 时，按 `LiukanshanLoader.start()` 的位置截断（为了去掉 demo 自启动），结果把原文件 IIFE 的闭合 `})();` 一起切掉了。内联进页面就是个语法错误，整段脚本不执行，`window.LiukanshanLoader` 挂不上，`app.js` 走了「拿不到动画就放行」的兜底分支 —— 表现为动画一闪而过、年份始终停在 HTML 写死的 `2026`，控制台常驻一条报错。

修法是截断后显式补 `\n})();\n`。验证方式：把提取出的代码丢进 `new Function(code)` 做语法校验，再用 DOM stub 模拟 20 帧，实测年份序列 `2010 2012 2015 2016 2018 2020 … 2026`，先快后慢、定格 2026，与设计一致。

**几何怎么验（09-15 换的方法，比手算靠谱得多）**

一律在**真浏览器**里量实际渲染包围盒（Playwright + 系统 Edge），不靠手推。脚本在 `_scratch/verify.py`，一条命令跑 12 种视口。

**判据要分开量实际墨迹，别量 `<g>` 的联合包围盒** —— 圆环布局下那个盒子是个斜长方形（同时框住内侧圆点和外侧那行字），相邻两个的角会蹭上，报「重叠 3.2px」，但墨迹根本没叠。这是踩过的度量坑。

| 判据 | 阈值 | 1440×900 实测 | 360×780 实测 |
|---|---|---|---|
| 字 ↔ 字 | ≥4px | 105.8px | 39.5px |
| 圆点 ↔ 圆点 | ≥0 | 87.8px | 16.9px |
| 字 ↔ 别人家的圆点 | ≥0 | 96.2px | 30.8px |
| 标签盒离框边 | ≥0 | 20.9px | 26.1px |
| 压到中心那句话 | 0px² | 0 | 0 |

12 组视口全过，控制台 0 error。完整报告见 `_scratch/report.txt`。

**教训：标签排布会「看着对、量出来不对」。** 上一版弧线布局里相邻标签一左一右混用锚点，会在中轴处**背靠背贴死**（两处实测：`x=336` 差 0.1px、`x=190` 差 0px），肉眼完全看不出来。所以现在整圈统一 `text-anchor: middle`，从构造上消掉这类缝。

因为几何全按框的宽高算，加了 resize 重排（160ms 防抖，保留当前选中年份）。

---

### 2.6 AI 层与独立部署（09-15 新增）

**为什么接 AI**：原来摘句子只按**长度**筛，跟「这句话在说什么」完全无关 —— 实测「买房是最好的投资」11 年里有 7 年抽中的句子和查询词零命中（详见 §7.1）。AI 只用在**两处模型确实强过启发式**的地方，而且都不产出要展示的文本。

**接的是什么**：一个 OpenAI 兼容端点 `https://api.openai-next.com/v1`，模型 `deepseek-v4-flash`。**只用标准库 urllib**，装到服务器上不需要 `pip install`。

**两条铁规矩（架构上保证，不是靠提示词）**：

1. **模型不许生成要展示的内容。**
   - 挑句子时它返回候选句的**下标**，句子本体是知乎原文，模型碰不到 —— 不可能编造引语。
   - 「换个说法再查」的候选**根本不经过模型**，用的是语料里真实存在的高赞标题（见下）。
   项目那条「不许添加来源没有的事实或引语」在接 AI 之后依然成立，而且是结构上成立。
2. **AI 挂了页面照常能用。** 所有 AI 调用失败都返回 None，调用方一律有启发式兜底。

| 接口 | 干什么 | 返回 |
|---|---|---|
| `GET /api/suggest?q=` | **输入推荐**：只发**一条**搜索（0.76s）就回 8 条真实标题 | `{ok, from, items:[{title,votes,url}]}` |
| `GET /api/parse?q=` | **只判断**输入是断言/话题/疑问/垃圾，不改写、不给候选 | `{ok, kind}` |
| `GET /api/picks?q=` | 在每年的候选池（5 句）里挑一句（返回下标） | `{ok, picks:{年份:{…}}}` |
| `GET /api/rings?q=` | 年轮数据，附带 `related`：语料里最高赞的 6 条真实标题 | `{…, related:[{title,votes,url}]}` |
| `GET /api/health` | 部署自检：AI 通不通、CLI 在不在、缓存几条 | `{ok, ai, model, base, zhihu_cli, cached}` |

**两段式交互（09-15 加的）**：

```
① 在首屏输入关键词（停顿 400ms）→ 推荐层出现，8 条真实标题    实测 0.9–1.6s
② 点其中一条 / 直接回车        → 完整检索，年轮画出来          实测 13s（实时）、0.01s（缓存）
```

第 ① 步之所以快，是因为它**只发一条**搜索调用，不是 `/api/rings` 那 15 条扇出 —— 这是这一整套里唯一能做到「输入就出」的地方，因为单次调用只要 0.76s。

推荐层是**绝对定位**挂在输入框下面的（`.hero-search{position:relative}`），所以出推荐时首屏的标题、副标题、示例按钮**一个都不动**，不会跳。

几个实现上的决定：
- **首屏和顶栏两个输入框都要挂这套逻辑。** 踩过一次：只挂了首屏那个，结果「搜过一次之后再输关键词就没有推荐了」—— 因为搜完首屏就藏起来了，能打字的是顶栏那个。现在两边共用 `initSuggest()`，样式也用同一个 `.sugbox`。
- **400ms 防抖**：一个词打下来只发一两次请求，不是每敲一个键发一次（每次请求都花一次知乎搜索配额）。
- **已整条搜过的词走语料**：`from:"cache"`，零调用。
- **空结果不落盘**：免得把一次网络抖动固化成「这个词没有推荐」。
- **失败静默**：推荐是增强项，挂了不挡输入、不挡回车。
- **绝对定位**：推荐层挂在输入框下面（`.hero-search` / `.tb-search` 都是 `position:relative`），出推荐时页面元素一个都不动，不跳。

**候选说法为什么不让 AI 生成（09-15 改的）**：

一开始是让模型把输入改写成 2–3 句可检索的断言。查知乎接口时顺手验了一下，发现**模型猜的跟语料对不上**：

- 对「张雪峰」，模型给的是「张雪峰的观点有事实依据」「张雪峰的言论被多数人认可」—— 它是凭旧知识泛泛猜的。
- 而这次实际捞回来的 106 条里，高赞的是「张雪峰到底靠不靠谱?」「为啥张雪峰的风评急转直下?」「张雪峰什么专业都懂?」。

模型不知道这次到底捞回来了什么，**语料知道**。所以改成：候选直接用 `related`（语料里真实的高赞标题，原文照搬、不改写）。好处是三重的 —— 更贴语料、零新增请求、不可能编造。AI 只保留「判断输入是哪一类」这一件事。

**顺带查过的：知乎有没有现成的「搜索补全」接口？没有。**

翻完 CLI 全部 22 个端点，**没有 autocomplete/suggest 端点**。能沾边的只有两个，实测结果：

| 命令 | 延迟 | 扣的配额 | 结论 |
|---|---|---|---|
| `question recommend` | 0.7–1.1s | **`creator`，总共只有 100 次** | 配额太少，做不了线上功能 |
| `search zhihu` | 0.9–1.0s | `zhihu_search`，5000 次 | 可用，但见下 |

延迟比 AI 端点好一个数量级，但两个都**只返回问句、不返回断言**。试过纯启发式改写（剥「吗/呢」、拆「是不是/该不该」），产出仍是问句 ——「大家如何评价张雪峰这个人」，等于把「莫名其妙」换了个形式。低频 n-gram 提取更糟，切出来的是「评价张 ×15」这种碎片。

**结论：搜索补全这条路，最可靠的「数据库」就是 ring 搜索本身已经拿回来的那批标题。**

---

### 2.7 速度：时间到底花在哪儿了（09-15 实测）

**一句话**：缓存命中 0.06s；实时检索 13–21s，其中**绝大部分不是网络、也不是知乎慢**。

| 项 | 实测 |
|---|---|
| 知乎单次搜索**裸延迟** | **0.76s**（不慢） |
| 缓存命中整条链路 | **0.06s** |
| **4 条并发**打出去 | **3 条直接 `Code 30001`** |
| 15 条串行，间隔 0.0s | **11.9s**，15/15 成功 |
| 15 条串行，间隔 0.3s | 15.5s，15/15 |
| 15 条串行，间隔 1.2s | 28.1s，15/15 |

**原来 1.2s 的间隔是白等的**，已降到 0.15s。现在实时检索 13–21s（抖动来自知乎侧的偶发慢响应）。

**为什么做不到「秒出」——和真正的搜索平台差在哪**：

| 搜索平台靠什么 | 我们为什么做不到 |
|---|---|
| **自己持有倒排索引**，查询是一次查表 | 我们是知乎搜索的**客户端**，每次「查询」都是一次远程往返（0.76s），而且要发 15 次 |
| **离线预计算**（抓取、建索引、算排序信号都在入库时做完） | 我们**全部在查询时做**：抓 → 去重 → 分年 → 打分 → 挑句 |
| **分片 + 并发 scatter-gather，设截止时间** | **实测并发直接 `Code 30001`** —— 物理上不能并行，这是最大的结构性差距 |
| **多级缓存**（CDN → 查询缓存 → 部分结果缓存 → 文档缓存） | 只有一级（按查询的文件缓存），但它是快的：0.06s |
| **渐进式渲染**：先出首屏，长尾慢慢补 | 现在是等 15 条全跑完才画。**这是剩下最大的一块可优化空间** |
| 预计算的前缀树做补全 | 没有 suggest 接口（见 §2.6） |

**所以能做的排序**：

1. ~~去掉 1.2s 间隔~~ **已做**，实时检索 19–28s → 13–21s，零风险。
2. ~~输入推荐~~ **已做**（09-15）：输入关键词 **0.9–1.6s** 出 8 条真实标题，因为只发一条搜索。见 §2.6。
3. **演示前预热**：所有要用的句子先查一遍 + `python warm_ai.py`，之后每次 **0.06s**。这是现场唯一真正靠谱的「秒出」。
4. **渐进式渲染**（未做）：第一条第 0.76s 就回来，先画一圈，13s 内补齐。**感知上从 13s 变成 ~1s**。这是把「等 15 条全跑完」改成「边跑边画」，需要把 `gather()` 改成可观测的增量任务（后台线程 + 前端轮询，跟 AI 那套一样的模式）。约 1.5–2 小时。
5. **减少查询条数**（15 → 8，约 7s）：代价是历史跨度变薄，某些年份没数据 —— 而「年轮」的价值恰恰在那个跨度上，不建议。
6. **把已查过的语料建成自己的倒排索引**：方向对（越用越快），但今天来不及。

**AI 调用一律不在 HTTP 线程里等。** 实测这个端点能从 **7 秒抖到 120 秒**，挂在请求上等的话反代会掐断、线程会被占死。所以是「起后台任务 → 立刻返回 `{pending:true}` → 前端轮询 → 结果落盘」。同一个 `(kind, q)` 只会有一份在跑，一分钟内失败过就不重试。

**踩出来的两个关键点**：

- **别让模型输出「理由」。** 第一版让它在挑句时附一句理由，同一个请求跑了 **240 秒还没回来**；去掉理由字段、只要 11 个下标之后，**7.6 秒**返回。输出长度是这里延迟的主因。
- **前端必须非阻塞。** 结果先按启发式画出来，AI 回来了再原地替换摘要（并打上「AI 挑句」标记）。拿几秒到几分钟的模型往返挡在搜索前面，换来的是"每次都多等一段"，不值。

**部署**（全部走环境变量，都有默认值）：

| 变量 | 默认 | 说明 |
|---|---|---|
| `HOST` / `PORT` | `127.0.0.1` / `8765` | 上线要改成 `0.0.0.0` 和真实端口 |
| `ZHIHU_CLI` | 本机 kanshan-workbench 那套路径 | 服务器上换成那边的路径 |
| `AI_BASE_URL` | `https://api.openai-next.com/v1` | |
| `AI_MODEL` | `deepseek-v4-flash` | 实测 `deepseek-v4-pro` 稍快一点 |
| `AI_API_KEY` | 无，读 `<项目根>/.ai_key` | 见下 |

**密钥处理（重要）**：

- 密钥在 **`F:\work palce\.ai_key`**，**刻意放在项目根、不放 `nianlun-web/` 里**。因为 `server.py` 把 `nianlun-web/` 整个当静态根往外 serve，密钥放里面的话，任何人访问 `http://站点/.ai_key` 就能把 key 拿走。
- `server.py` 里另有一道兜底拦截：任何 `.` 开头的路径、`cache/`、`__pycache__/`，以及 `.py/.pyc/.mjs/.md` 后缀一律 404（已实测）。
- 上线时优先用环境变量注入，别把这个文件一起传上去。

**预热**：AI 首次跑要 8 秒到 2 分钟（端点抖动大）。演示前跑一次：

```powershell
cd "F:\work palce\nianlun-web"
python warm_ai.py                  # 把 cache/ 里已有的话全部预热
python warm_ai.py 买房是最好的投资   # 或只预热指定几句
```

之后每次搜索都是 0.01 秒命中缓存。

---

## 3. 怎么重新生成

```powershell
# ── 加载动画 ──
cd "F:\work palce\liukanshan"
node gen-loader.mjs                 # → liukanshan-loader.html

# ── 封面（三选一主体）──
cd "F:\work palce\poster-v2"
node poster-line.mjs                # 默认 = wire 版（官方）
node poster-line.mjs gen            # 用户生图的推沙漏版 ← 当前主推
node poster-line.mjs desert         # 官方沙漠版（已被否）

# ── 素材重处理（换了生图原图之后）──
cd "F:\work palce\gen-assets"
# 先把新原图存成 fox-hourglass-hi.jpg，再：
python _remap.py                    # → fox-hourglass.png，封面会自动用上

# ── 网页正片 ──
cd "F:\work palce\nianlun-web"
node build-index.mjs                # → index.html（改样式/结构后必跑）
python server.py 8765               # → http://127.0.0.1:8765
python warm_ai.py                   # 演示前跑一次：预热 AI 结果（见 §2.6）

# 起来后先自检：
#   http://127.0.0.1:8765/api/health
#   → {"ok":true,"ai":true,"model":"…","zhihu_cli":true,"cached":6}

# ── 上线（环境变量，全套见 §2.6）──
#   $env:HOST="0.0.0.0"; $env:PORT="8080"; $env:AI_API_KEY="sk-…"; python server.py
```

**改文案/数据**：`poster-v2/poster-line.mjs` 顶部 `ROWS` 数组，每项 `[年份, 引语, 百分比, 出处]`。第四项会**自动写进封面 HTML 的核对注释**。

---

## 4. 怎么验证

三个脚本，都是单条命令，都会打印 PASS/FAIL：

```powershell
cd "F:\work palce\poster-v2"
python antislop-audit.py gen        # 设计闸门 12 项 + 对比度 + 出图
python copy-audit.py                # 文案闸门 9 项（本次打回过两次）

cd "F:\work palce\liukanshan"
python verify-loader.py             # 动画版式契约（对齐/错开/裁切/控制台错误）
python shoot-windows.py             # 三种视口实机截图
```

**判定标准（当前全部通过）**：

| 项 | 阈值 |
|---|---|
| 画布 | 315 × 175（内容底边 ≤175，`scrollHeight == clientHeight`） |
| 对比度 | 正文 ≥ 4.5:1（现：灰 4.59 / 蓝 5.34 / 墨 16.74） |
| 设计闸门 | 12 项（字体非系统默认 / 无渐变 / 非纯白纸 / 非零彩度 / 字族≤3 / 标题非斜 / aria / overflow-clip 等） |
| 文案闸门 | 9 项（句首句尾不重复 / 句长有差 / 句式有变 / **无数字年代人名金额** / 无破折号 / 弯引号 / 无 AI 词 / 无销售语言） |
| 动画 | 图层 transform 恒定、成品包围盒冻结、中轴偏差 ≤1px、十种视口不裁切、0 控制台错误 |

**网页正片：现在有脚本了**（`_scratch/verify.py`，09-15 新增）

```powershell
cd "F:\work palce\nianlun-web"

# 1. JS 语法
node --check app.js

# 2. 开场动画脚本是否完整（IIFE 闭合曾被截断过，见 §2.5 的坑）
node -e "const fs=require('fs');const h=fs.readFileSync('index.html','utf8');const i=h.indexOf('/* 刘看山打开动画的原始 API');new Function(h.slice(i,h.indexOf('</script>',i)));console.log('动画脚本语法 OK')"

# 3. 几何 + 交互：12 种视口实机跑一遍，量墨迹、截图、验滚动
#    需要 Playwright + 系统 Edge，服务得先起着
#    受限沙箱下会 WinError 5（命名管道被禁），要放开权限跑
python "F:\work palce\_scratch\verify.py"
#    → 逐行打印 PASS/FAIL，完整报告写进 _scratch/report.txt
```

判据见 §2.5 的表格。**必须跑真浏览器** —— 这些数字手算不出来，实测推翻过我两次判断。

浏览器里再人工看三件事：开场年份确实从 2010 爬到 2026；动画淡出后首屏**不消失**（踩过两次）；点年份会滚到文章区且内容跟着换。

---

## 5. 关键设计决策与依据

| 决策 | 依据 |
|---|---|
| 封面走**线稿 / 涂鸦**风（而非 3D 霓虹或水彩） | 三张参考图里只有这张是**矢量线稿**，能完整用 HTML/SVG 还原；另两张的原生载体是生图 |
| 标题用**粗宋 + 正体** | hallmark 闸门 #1（禁用系统默认显示字体）+ #38a（**斜体标题是顶级 AI 特征**，原稿带 `skewX(-7deg)`） |
| 纸色不用纯白 `#FFFFFF`，改 `#FBF9F4` | 闸门 #7 + #22（纯白 = 零彩度死板中性色） |
| 灰/蓝都调深 | 闸门 #40：原灰 3.18:1、原蓝 3.66:1，**都不合格**；现 4.59 / 5.34 |
| 时间轴四句**不举具体事例** | humanizer 规则有优先级：「不许添加来源没有的事实/引语」是**工作准则**，「要具体」是**风格偏好**，准则高于偏好。本环境又查不到可核实的出处（见 §6） |
| 于是反 AI 味从"细节"移到"结构" | 四句是**四种言语行为**（断语/疑问/不确定/许可），首尾字互不重复，句长 5/5/6/4，四句**零事实断言** |
| 动画里数字与主体**上下错开** | 逐像素实测：同心时中间的「0」只剩 **2%** 可见、「2」剩 41%；错开后全部 100% |
| 动画用**相对位移**不用负外边距 | 负外边距会把插画一起拉上来（"移动数字"和"移动主体"在视觉上不是一回事，用户指出过） |

---

## 6. 环境边界与坑（重要，能省你很多时间）

**这个环境查不到中国互联网内容**，实测记录：

| 通道 | 结果 |
|---|---|
| 知乎主站 / 搜索 / 专栏（三种方式） | **403 反爬** |
| 百度百科（四个词全试） | **403** |
| 中文维基 | DNS 被解析到非公网地址，取不到 |
| 人民日报 2016 头版档案 | **403** |
| gov.cn / 新华网 / 央视网 | 首页可达，但**搜索引擎给的深链 3 条全部 404** |
| **搜索引擎返回的"来源链接"** | **可能整个是拼出来的** —— 三次 fetch 全 404。**不 fetch 一遍不能信** |
| 百度 SERP | 200，但结果区 JS 渲染，只拿得到"相关搜索" |
| GitHub API | 通，但**按 IP 限流**（shell 直连时 `rate limit exceeded`）；走 `web_fetch`/`read_page` 可以 |
| `raw.githubusercontent.com` | 解析不了；用 `api.github.com` 的 blob 接口或 `read_page` 可绕 |
| jsDelivr | 会 302 到 `raw.githubusercontent.com`，跟着失败 |

**结论**：想核实"真实引语"必须在**你自己的浏览器**里做（你能开知乎），本环境无解。

---

## 7. 未决事项（需要你拍板）

### 7.1 总结句跟用户输入对不上 ★ 现在最该动的地方

**现象**：摘出来当「这条回答说了什么」的那句话，经常跟用户输入毫无关系。2026 年显示的是「希望表哥能早点重拾信心，度过难关吧」，看着莫名其妙。

**实测证据**（`_scratch/diag_summary.py` 跑的，逐字全量在 `_scratch/diag-summary.txt`）：拿「买房是最好的投资」缓存里 11 年逐年比 —— **7 年抽中的句子命中查询词 0 个**，而这 7 年里每一年都存在命中 4–7 个词、赞数高得多的句子：

| 年 | 现在抽中的 | 命中词 | 那年其实有的句子 |
|---|---|---|---|
| 2016 | 从去年就开始吹风，今年只是落实了而已 | **0** | `[7词/1赞]` 过去十六年的历史证明，**买房是最好的投资** |
| 2020 | 如果在市区好地段有新房开盘… | **0** | `[4词/618赞]` 没用父母一分钱…这辈子目前**最好的投资** |
| 2021 | 当时确实是个美国导致的经济周期错配… | **0** | `[7词/0赞]` 绝大多数国人仍然认为**买房是最好的投资** |
| 2022 | 鱼的记忆只有七秒，而大众的记忆呢 | **0** | `[7词/97赞]` 我以前不太懂得**买房，是最好的投资** |
| 2024 | 作为一个买了12年房的老炮… | **0** | `[3词/2546赞]` 对核心城市，2024年是未来五年内**最好的买房时机** |
| 2026 | 希望表哥能早点重拾信心，度过难关吧 | **0** | `[6词/2518赞]` 那十年，**买房就是最好的投资** |

**三个独立的根因**：

- **D1 输入不校验**：用户输入什么都直接当观点用，于是 15 条查询词全围绕一个可能根本不是观点的东西构造。缓存里「张雪峰是对的吗」「张雪峰说的对吗」就是用户自己撞上的 —— 那是疑问句，不是可检索的断言。
- **D2 抽句跟「说的是什么」无关**：`server.py` 的 `pick_sentence()` 只按**长度**筛（12–50 字、跳过「首先/其次」这类开头），完全不看这句话跟查询词有没有关系，也不看它是不是个人闲话。
- **D3 语义跳跃**：把「某条回答里的一句话」当成「那一年的观点」展示。就算抽中了 2026 那句「那十年，买房就是最好的投资」，它也只是**某个人的一句话**，代表不了 2026 年。

**09-15 已经做的**：

- **方案一 · 已做，而且分了两层。**
  - **启发式层**：`pick_sentence` 旁边加了 `year_candidates()`，按「命中查询词数 → 点赞数」排序。**光这一层就治好了主症**：「买房是最好的投资」2026 年从「希望表哥能早点重拾信心，度过难关吧」变成了「那十年，买房就是最好的投资」；2016 从「从去年就开始吹风，今年只是落实了而已」变成了「过去十六年的历史证明，买房是最好的投资」。
  - **AI 层**：再让模型在每年的 5 句候选里挑一句，只返回下标。实测「女性不被定义」8 年里 AI 改了 4 年，4 处都是明显更好：
    - 2020：启发式「2020年夏天，国内综艺与电视剧将"女性由谁来定义」（半截话）→ AI「女性不是被定义，她们定义自己并为自己叙事」
    - 2021：启发式在解释「妇女」这个词的用法 → AI「如果我们都把"妇女"贬义化，避而不谈，那么女性就更有可能被随意定义了」
    - 2026：启发式「你别说，2022年还真出版了一本《不被定义的女性》」（岔话）→ AI「这就是为什么女性主义喊了一百多年"独立""自主""不被定义"，结果却是女人们更加精确地被定义」
- **前端口径也改了 —— 这一条直接治 D3。** 不再写「摘自当年最高赞」（AI 挑的可能不是最高赞那条），改成「**当年 N 条里，说得最直白的一句**」+ 作者，AI 挑的另打一个「AI 挑句」标记。句子从此不再冒充「那一年的观点」，只是「某个人的一句话」。
- **方案二 · 做了一半（非阻塞版）。** `/api/parse` 判断输入是断言/话题/疑问/垃圾，不是断言时结果上方出现一条候选 —— **候选是语料里真实的高赞标题，不是模型生成的**（09-15 改的，理由见 §2.6）。点一条就按那条重搜。**刻意不做成阻塞式确认**：结果先照原话出，建议条只是提示，点不点都行 —— 拿几秒到几分钟的模型往返挡在搜索前面不值。代价是这类输入仍然先浪费一次检索。
- **方案四 · 部分达成。** `cache/` 六条 + `warm_ai.py` 预热，演示走缓存时**秒回且不会抽歪**。首屏示例按钮仍是四条硬编码，没跟 AI 候选打通。

**还没做的**：

- **方案二的「阻塞式确认」**：现在非断言输入仍然先搜一次原话（结果是垃圾），用户得点建议条再来一次。做成"先确认再搜"能省一次无效检索，代价是每次都多等一段模型往返。**这是个体验取向的选择，得你拍板。**
- **方案三（正反两方各一句）**：没动，年轮上每年仍然只出一句。
- **§7.2 #1 的首屏示例按钮**：仍没跟 AI 打通。

---

### 7.2 其余事项

> 09-14 更新：封面已确认**只用于报名，不进网站**，所以原 #1、#4 两条封面相关的降级为「可不动」。

| # | 事项 | 现状 | 可选做法 |
|---|---|---|---|
| 1 | **网页首屏示例按钮** | 只列了四条，缓存里有六条。但多出的两条（「张雪峰是对的吗」「张雪峰说的对吗」）是**疑问句不是观点** —— 见 §7.1 D1，直接放进示例等于教用户输入错的形态 | 先不动。若做 §7.1 方案二，这排按钮改成由模型给候选观点，那时一并重写 |
| 2 | ~~1280 宽下边距只剩 2px~~ | **已解决**：年轮改成圆环后那套弧线参数整个换掉了，1280×800 实测最小边距 20.9px | 无需动作 |
| 3 | **调试遗留文件** | `nianlun-web/` 下有 `t2.json`、`test.json`、`_t.html`；`_scratch/` 下有本轮的验证脚本、截图与报告 | 前三个可删；`_scratch/` 建议留着（`verify.py` 补上了 §4 缺的验收脚本），要删说一声 |
| 4 | ~~封面时间轴四个百分比~~ | 是示意值，但封面不进网站 | 报名已用，可不动；若要改见 §2.2 的 `ROWS` |
| 5 | ~~封面右上两个小装饰~~ | 四角星 + 雪地脚印 | 同上，可不动 |
| 6 | **刘看山素材** | 动画/封面默认 wire 版；desert 版**已否**（去年别人用过）。开场动画用的是 desert 版线稿 | 继续按 `gen-refs/prompt-global.md` 生图，**记得导出原图、不要缩略图**（上一版是带水印的 384×216，可用分辨率只有 1/4） |
| 7 | **AI 密钥的保管（09-15）** | 明文在 `F:\work palce\.ai_key`，**只有一个 key，没有做额度/频率保护** | 上线时改成环境变量注入、不要把文件传上去；公开部署的话建议在服务端加一层按 IP 的限流 —— 否则别人可以拿你的站点刷这个 key |
| 8 | **AI 冷启动要等** | 没预热过的句子，第一次访问要等后台跑完（8 秒到 2 分钟），期间页面显示的是启发式结果 | 演示前跑 `python warm_ai.py`；或把 `cache/_ai/` 一起带到服务器上 |

---

## 8. 外部依赖与许可

| 依赖 | 用途 | 许可 |
|---|---|---|
| Noto Serif SC 900 | 封面标题（子集化 4 字，1.7 KB，内嵌） | SIL OFL |
| Bodoni Moda | 动画年份数字（子集化 0-9，3.7 KB，内嵌） | SIL OFL |
| 官方刘看山线稿（desert / wire） | 动画与封面主体 | **知乎版权**。注意[品牌指南](https://www.zhihu.com/brand)写明禁止擅自修改矢量文件 —— 当前做法是把描边统一成墨色（desert 原色 `#525C76` / `#82899C`，wire 原色 `#515B75`），属于轻度修改，若要完全合规可保留原始蓝灰 |
| 用户生图（推沙漏刘看山） | 封面主推主体 | 用户自有 |
| OpenAI Next Credits（`api.openai-next.com/v1`，模型 `deepseek-v4-flash`） | 判断用户输入形态、在候选句里挑一句（09-15 接入） | 第三方 API，key 在项目根 `.ai_key`；**只用标准库 urllib，无 pip 依赖** |
| ~~Tabler Icons 沙漏~~ | 曾用于 tagline，**已删除**（闸门 #30：混用两套图标来源） | MIT（已不依赖） |

| 设计决策（09-15 新增） | 依据 |
|---|---|
| **AI 只做「判断」和「选择」，不做「生成」** | 挑句时让它返回**下标**而不是句子，句子本体始终是知乎原文 —— 这样「不许添加来源没有的事实或引语」那条工作准则在**架构上**就不可能被违反，不依赖提示词自觉 |

**反 AI-slop 判据来源**（都已读过原文）：
- [`Nutlope/hallmark`](https://github.com/Nutlope/hallmark) · MIT · `references/slop-test.md` = **58 条闸门 + 6 维预审**
- [`blader/humanizer`](https://github.com/blader/humanizer) · MIT · 源自[维基百科《Signs of AI writing》](https://en.wikipedia.org/wiki/Wikipedia:Signs_of_AI_writing)
- `Asher-2000/dsh-artist-mode` · MIT · 已提取到 `_artist-design/`

---

## 9. 目录结构

```
F:\work palce\
├─ HANDOVER.md                  ← 本文
├─ liukanshan\                  ← 加载过场动画（主交付之一）
├─ poster-v2\                   ← 项目封面（主交付之二）+ 三个主体版本 + 三套审查脚本
├─ nianlun-web\                 ← 网页正片（主交付之三）
│    ├─ build-index.mjs         ← 生成器：产出 index.html
│    ├─ index.html              ← 生成产物，别手改
│    ├─ app.js                  ← 状态机 + 圆环几何 + 绘制 + 两条 AI 增强
│    ├─ server.py               ← 服务端：知乎检索 + 抽句 + AI 接口
│    ├─ ai.py                   ← AI 层（09-15）：OpenAI 兼容客户端，只用标准库
│    ├─ warm_ai.py              ← AI 预热工具（09-15）：演示前跑一次
│    ├─ liukanshan-loader.html  ← 开场动画原件（生成器的输入）
│    └─ cache\                  ← 六句话的真实检索存档
│         ├─ _ai\               ← AI 结果缓存（挑句 / 输入形态），已预热
│         └─ _sug\              ← 输入推荐缓存（按关键词），已预热
├─ .ai_key                      ← **API 密钥（09-15）**。刻意放项目根不在 nianlun-web/ 里
├─ _scratch\                    ← 09-15 新增：验收与诊断脚本（非交付，但有用）
│    ├─ verify.py               ← 12 种视口的几何/交互实测（§4 用它）
│    ├─ report.txt              ← 上面那个脚本最近一次的输出
│    ├─ diag_summary.py / .txt  ← §7.1「抽句抽歪」的证据
│    ├─ test_api*.py / api-test*.txt  ← AI 接口端到端实测
│    └─ new-*.png / ai-*.png    ← 实机截图
├─ gen-assets\                  ← 用户生图的处理成品与脚本
├─ gen-refs\                    ← 生图提示词与参考图
├─ zhihu-ring\                  ← 往届赛事研究存档
├─ _artist-design\              ← 早期提取的设计技能文档
├─ nianlun-video\               ← 早期视频渲染（未采用）
├─ poster\                      ← 更早一版海报（已被否，保留对照）
└─ archify-demo\ xianyu-*\ _dsh_settings_backup\ obc-config-*.toml
                                 ← 与本项目无关的历史文件
```

---

## 10. 盘点差异（请注意）

### 09-12 盘点时发现的（仍待你确认）

1. **多出来的文件**：`pelican-bicycle.html` / `pelican-bicycle.svg` / `pelican-preview.png`（2026-09-12 17:54–18:04）—— 不是本会话产物。
2. **不见了的文件**：`_sess_latest.jsonl`、`_peon_packs.tgz`、`_base.py.mine`、`_openai_provider.py.mine`、`_obc_local_patches_20260904-092124.patch` —— 早先盘点时存在，现在不在。**这些不在我的删除范围内**（我删的是 archify、liukanshan-hf、liukanshan-lottie、dsh-figma-to-lottie、bili-data 与 41 个临时脚本）。

如果这两项不是你自己做的，建议查一下是否有其他会话/工具在同一工作区里动过文件。

### 09-15 新增（本轮）

**接好了独立 AI，抽句硬伤修掉了**，详见 §2.6 和 §7.1。要点：

1. **真实的 AI 端点地址不是文档域名**。`https://credits.openai-next.com` 那个站是产品页，`/v1/*` 走的是 Next.js 前端（返回 HTML 404）；真正的 API 在 **`https://api.openai-next.com/v1`**（从它自己的 JS chunk 里挖出来的）。模型 id 也是 **`deepseek-v4-flash`**（带连字符），写 `deepseekv4flash` 会返 503。`GET /v1/models` 列得到 640 个模型。
2. **别让模型输出「理由」**。挑句时附理由 → 同一个请求跑 240 秒没回来；只要 11 个下标 → 7.6 秒。输出长度是这个端点延迟的主因。
3. **这个端点抖动极大**：实测单次调用在 **3.5 秒到 286 秒**之间。所以 AI 一律后台跑 + 落盘缓存 + 前端轮询，绝不挂在 HTTP 请求上等。
4. **密钥放在项目根 `F:\work palce\.ai_key`**，不放 `nianlun-web/` —— 后者是被静态 serve 出去的，放里面等于公开。`server.py` 另加了兜底拦截（`.` 开头路径、`cache/`、`__pycache__/`、`.py/.pyc/.mjs/.md` 一律 404，已实测）。
5. **端口上的坑**：Windows 上 Python 的 `allow_reuse_address` 允许**多个进程同时绑同一个端口**，于是历史上遗留的服务进程会和新起的抢答，出现「代码改了但行为没变」。排查用 `netstat -ano | findstr :8765`，全杀掉再起。
6. **「搜索补全」不靠 AI 了**。翻完知乎 CLI 全部 22 个端点，**没有 suggest/autocomplete 接口**；`question recommend` 虽然快（0.7s）但扣的 `creator` 配额总共只有 100 次，而且和 `search zhihu` 一样**只返回问句、不返回断言**（纯启发式改写试过，产出的还是问句）。最后改成用 ring 搜索自己已经拿回来的高赞标题 —— 更贴语料、零新增请求、不可能编造。详见 §2.6。
7. **顺手验出一个反例**：对「张雪峰」，模型凭旧知识猜的候选（「张雪峰的观点有事实依据」）跟语料里实际在谈的（「张雪峰到底靠不靠谱?」）**完全对不上**。这是「模型不知道你这次捞回来了什么」的直接证据。

**新增 `_scratch/`**：本轮的验收脚本、诊断脚本、截图和报告。不是交付物，但 §4 的验收现在是靠它跑的。

---

### 09-14 白天新增（我留下的）

`nianlun-web/` 下三个调试遗留文件：`t2.json`（18 KB，早期 live 检索结果）、`test.json`（76 B）、`_t.html`（浏览器端探针页）。都可删，不影响运行 —— 没自行删除是因为删文件要你确认。

缓存目录从四条长到六条，多出的「张雪峰是对的吗」「张雪峰说的对吗」是你自己搜出来的，都是真实返回 —— 顺便说，这两条正是 §7.1 D1 说的「用户输入的不一定是观点」的实例。

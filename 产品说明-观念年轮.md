# 观念年轮 · 产品说明

> 知乎黑客松 2026｜校园新锐季 · 知识炼金场｜学习工具与知识生产
> **一句话定位：你的观点，还剩多少保质期**

---

## 一、创作思路

### 起点是一个具体的生活观察

每个人脑子里都装着一批从没说出口的常识 —— 买房是最好的投资、读研一定比本科好找工作、孝顺是美德。它们大多不是我们自己想出来的，是某个年代被反复讲、讲成了默认设置。

而**没有人知道自己信的那句话，是哪一年的共识**。

事实可以查证，共识没有时间戳。这是学习工具一直没有解决的一件事。

### 第一个转折：搜索解决不了

最初的设想很朴素 —— 查一个说法的历年讨论。但一上手就发现直接用搜索做不到：**搜索引擎只给"现在大家怎么说"**。实测过，单发一条查询，49 条结果里 40 条落在当年。画出来是"今年的横截面"，不是年轮。

要做年轮，就必须拿到**按年分布**的语料。而在中文互联网上，**只有知乎有这个东西**。

### 第二个转折：立意的收紧

做到一半时接入了大模型，很自然会想让它"总结一下那一年的观点"。我们试了，然后退回来了。

因为一旦让模型生成要展示的文本，整个产品就变成了"又一个 AI 告诉你结论的东西"。而**这个时代最不缺的就是看似合理的结论**。

于是立意收紧成一句：

> **不给你结论，给你一条能被你自己验证的时间线。**

这句后来变成了整个产品的架构约束 —— 见第四节。

---

## 二、产品是什么

输入一句你信以为真的话，**观念年轮**去知乎把这句话历年的讨论捞回来，按年份排成一个年轮。

- 从正上方起，**顺时针一年一格**，覆盖全部有讨论的年份
- **点越大 = 那年谈的人越多**（2026 年的 86 条明显大过 2017 年的 3 条）
- 点年份，下方展开那一年的原文，**每条带作者、赞数、和直达知乎的链接，点开就能核**

**怎么用（30 秒）**：

1. 首屏输入关键词 → 约 1 秒列出知乎上真实存在的高赞说法，点一条
2. 年轮画出来 → 一眼看出这个话题从哪年开始、哪年最热、什么时候转向
3. 点年份看原文，点链接直达知乎核对

真实样例：「买房是最好的投资」→ 206 条讨论 / 11 圈 / 跨 2016–2026。年轮会把**转向**讲出来 —— 2016–2017 是"买房是最好的投资"，2020–2025 变成"不要投资买房，风口过去了""房子在当下是垃圾资产"。**一句话的保质期，肉眼可见。**

---

## 三、适配知乎社区的场景价值（重点）

### 1. 盘活知乎最厚的那层资产

知乎真正的壁垒不是"能搜到答案"，而是**十几年沉淀下来的、带时间戳的、可追溯的中文公共讨论**。微博是碎片且易逝，公众号是封闭的，搜索引擎只索引不组织。

但现在的产品形态 —— 搜索、推荐、热榜 —— 都在消费**当下**。老内容只能被动等待被搜到。

**年轮第一次让"历年沉淀"本身成为一个产品形态**：每一圈都是一次老内容的重新曝光，而且是带上下文、带归属、带链接的曝光。用户看到的不是"一条老回答"，而是"这个话题是怎么一路走过来的"。

### 2. 流量回流原回答，而不是把内容搬走

这是和市面上多数"AI 总结知乎"的工具最根本的区别：

- 我们**不生成内容**，只把原文重新组织
- 每一条都带**作者名、赞数、原文链接**，点开直达知乎
- 用户看到的是一个**发现老作者的入口**：这个话题 2018 年是谁在说得最清楚？点进去，可能就关注了

**它给知乎做的是长尾分发，不是内容套利。**

### 3. 它显式化并校准了社区的一种结构性偏差

知乎的排序机制把高赞推到最上面，而**高赞反映的是当下的共识**。这带来两个必然的误判：

- **把旧共识当新共识** —— "买房是最好的投资"在 2026 年的真实分布是 37 条偏质疑 : 12 条偏支持，但用户凭印象仍会当成"大家都这么说"
- **把少数派当主流** —— 某个说法在特定年份其实只有零星几条，看起来却像是普遍认同

年轮把这个偏差摆到台面上：**高赞 ≠ 长期正确，共识是会过期的。** 这是社区内容生态的一次自我校准，也是知乎作为知识社区最该主动告诉用户的一件事。

### 4. 它给讨论装了一根时间轴，鼓励更高质量的发言

当用户能看到"这个话题 2018 年是怎么被论证的、2024 年又是怎么被推翻的"，讨论会自然从情绪转向论据。年轮提供的是一个**论据的坐标系**：你想说一个新观点，先看看历史上它被说过没有、被怎么反驳过。

### 5. 它是知乎内容形态的"专属应用"

这个产品**换一个平台就做不出来**，不是技术问题，是数据形态问题：

| 需要的数据特征 | 知乎 | 典型替代 |
|---|---|---|
| 长文说理（能抽出一句完整立场） | ✅ | 微博 140 字碎片 ✗ |
| 明确的时间戳（能分年） | ✅ 编辑时间 | 多数平台不公开 ✗ |
| 作者归属（能回链、能发现人） | ✅ | 匿名社区 ✗ |
| 社区排序（能看共识强度） | ✅ 赞同数 | 无排序 ✗ |
| 十几年连续覆盖 | ✅ | 新兴平台 ✗ |

**知乎是唯一同时满足这五条的语料源。** 这个产品本质上是"把知乎的独特数据结构，翻译成一个普通用户能看懂的图形"。

### 6. 可自然长成社区功能

年轮不只是一个独立站点，它有明确的嵌入式落点：**话题页、搜索结果页、争议性说法的讨论页**。形态可以是一行小字 ——

> 「买房是最好的投资」在知乎被讨论了 11 年，共 206 条。**共识在 2020 年发生转向 →**

---

## 四、核心原则：不编造，是硬约束不是风格偏好

凡是"看起来很像真的"的便利，一律让位给"能被核实"。这一条不只写在文案里，**它被写进了架构**。

- 年轮上的每一句话，都是**从知乎原文里整句抽出来的** —— 不生成、不改写、不拼接
- **接了大模型，但模型不产出任何要展示的文本。** 它只做两件事：判断用户输入是断言/话题/疑问，以及在已有的候选句里"挑一句"。**它返回的是下标** —— 句子本体是知乎原文，模型碰不到，所以**在架构上就不可能编造引语**
- 「换个说法再查」的候选，直接用语料里真实的高赞标题，不经过模型
- **正反两向都投**：检索时同时投「{说法}」和「{说法} 是错的」等反向词，让同一年的支持与质疑都进池子。只留下说对的那几个，等于骗人
- 立场（偏支持 / 偏质疑 / 两说）是**数关键词的启发式**，界面上明写"不作数"，不当统计结论卖

---

## 五、技术方案

```
静态前端  ──►  轻量服务端  ──►  知乎开放平台（真实数据）
                    └──►  大模型（只做判断与选择，不产出文本）
```

| 层 | 实现 |
|---|---|
| 前端 | 单文件 HTML/CSS/JS，**零外链**；年轮是手写 SVG，几何按视口实时解算 |
| 服务端 | Python 标准库，**无第三方依赖**；全部配置走环境变量，可独立部署 |
| 数据 | 知乎开放平台 CLI（`zhihu-cli search zhihu`），官方接口、真实返回 |
| AI | deepseek-v4-flash（OpenAI 兼容端点），仅标准库 urllib 调用 |

### 数据链路

```
用户输入
  ├─► /api/suggest   只发 1 条搜索 → 0.9~1.6s 列出真实高赞说法（输入推荐）
  └─► /api/rings     扇出 15 条查询 → 合并去重 → 按年分桶 → 抽句 → 年轮
        ├─► /api/parse   判断输入形态（断言/话题/疑问/垃圾），只分类不改写
        └─► /api/picks   在每年的候选句里挑一句，只返回下标
```

### 关键工程决策（都来自实测，不是拍脑袋）

1. **一句话扇出成 15 条查询再合并去重。** 只发一条的话，知乎强烈偏向近期 —— 实测 49 条里 40 条落在当年，画出来是"今年的横截面"，不是年轮
2. **正反两向都投。** 正向词（`{q}`、`当年 {q}`）+ 反向词（`{q} 是错的`、`不认同 {q}`）+ 年份探针（`2015年 {q}`…），让同一年的支持与质疑都进池子
3. **串行执行、撞限流立刻收手。** 实测限流卡的是**并发**（4 条并发 → 3 条被拒），不是串行速率（15 条零间隔串行全部成功）。去掉那个想当然的 1.2 秒间隔后，扇出从 28 秒降到 12 秒
4. **AI 一律后台跑 + 落盘缓存 + 前端轮询。** 模型端点实测延迟在 **3.5–286 秒**之间剧烈抖动，绝不能挂在 HTTP 请求上等
5. **AI 全是增强项。** 挂了页面照常能用，因为启发式结果早就在页面上了
6. **抽句先按"命中查询词"排序**（启发式），再由 AI 在候选池里挑。实测这一步把"抽歪"从常态变成个例 —— 原本 11 年里有 7 年抽中的句子跟查询词零命中

### 几何方案：不调参，直接解

年份标签绕整圆均匀排开。难点是标签是**横排两行字** —— 在上下两端吃掉的是它的高度、在左右两端吃掉的是它的宽度，差一倍多。所以逐年按它自己的角度算标签盒在半径方向的半投影，取最紧的那个当半径上限：

```
reach = 节点半径 + 间隙 + (|cosθ|·半宽 + |sinθ|·半高)
R ≤ (cx - margin - 半宽)/|cosθ| - reach
R ≤ (cy - margin - 半高)/|sinθ| - reach
```

这样圆的每一段都恰好顶到框的边界，不浪费也不越界。

### 验收

**12 种视口全部实机验收通过**（1920/1600/1440/1280/1100/1024/768/390/360 共 12 组）。判据是量**实际渲染墨迹**：字↔字、圆↔圆、字↔别家圆均无重叠，标签不出界，控制台零 error。

*（踩过的坑：一开始量的是 `<g>` 的联合包围盒，圆环布局下那是个斜长方形，相邻两个的角会蹭上，报"重叠 3.2px"，但墨迹根本没叠。度量口径错了会把好设计判成坏的。）*

---

## 六、当前完成度

**能跑，不是原型图。**

| 项 | 状态 |
|---|---|
| 加载过场动画（刘看山，年份 2010→2026 滚动） | ✅ 已交付，单文件零依赖、字体内嵌 |
| 项目封面 315×175 | ✅ 已交付 |
| **网页正片：检索 → 聚合 → 年轮 → 原文 → 直达链接** | ✅ **全链路可跑** |
| 真实数据 | ✅ 15 条知乎真实检索存档，最大一条 206 条讨论 / 11 年跨度 |
| 输入推荐 | ✅ 0.9–1.6 秒（只发一条搜索） |
| AI 挑句与输入判断 | ✅ 已接，结果落盘预热，页面秒开 |
| 独立部署能力 | ✅ 环境变量全配置化，无 pip 依赖 |
| 12 视口几何验收 | ✅ 全 PASS |
| 速度 | 缓存命中 **0.06 秒**；实时检索 13–21 秒 |

---

## 七、后续计划

1. **把已查过的语料建成自己的倒排索引。** 现在的实时检索受限于知乎接口（单次 0.76 秒 × 15 条），做不了真正的"秒出"。长期看，本地索引 + 离线预计算才是解法
2. **每年改成"正反两方各一句"。** 进一步消除"一句话冒充一整年共识"的错觉
3. **让用户标出自己信的那一年。** 从"看别人的年轮"变成"标出自己的保质期"，把工具变成习惯
4. **嵌入知乎话题页 / 搜索结果页。** 作为社区功能落地，见第三节第 6 点
5. **接入更多语料源。** 目前只有知乎，长尾话题的年份覆盖会偏薄

---

## 八、为什么值得做

它解决的不是"查得到查不到"，而是**"我凭什么相信我信的东西"**。

在一个所有人都在教你怎么提问、怎么找答案的时代，它做的是相反的事：**帮你看清你已有的答案，是哪一年的。**

> 每一个常识，都曾经是少数派。
